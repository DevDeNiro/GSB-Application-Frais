<!DOCTYPE html>
<html>
  <head>
    <title>GSB | Galaxy Swiss Bourdin</title>
    <meta charset='utf-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    {% block stylesheets %} 
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" crossorigin="anonymous">
            <link href="{{asset ('styles/main.css') }}" rel="stylesheet"/>
            <link href="{{asset ('styles/all.css') }}" rel="stylesheet"/>
            {% endblock %}
  </head>
  {% block body %}
    <div class="container-fluid px-0 hauteur2">
      
      <div class="row hauteur2">
        <div class="col-2 menu_latéral p-0">
            <header>
              
              <a href= {{path ('accueil_visiteurs')}}>
                {% if app.user is null %}
                  <h3 class="my-3">Aucun utilisateur connexté</h3>
                {% else %}
                  <h3 class="hauteur bienvenue">Bienvenue {{ app.user.prenom }}</h3>
                {% endif %}<span class="id_user">
              </a>
              
            </header>
            <div class="ml-3 nav w-auto pl-3 mt-4">
                <div class="p-0 pb-2 col-12 liste  ">
                  <a href= {{path ('accueil_visiteurs')}} class="p-0" >
                    <i class="fas fa-home"></i>  Accueil
                  </a>
                </div>

                <div class="p-0 pb-2 col-12 liste  ">
                  <a href= {{path ('choix_fiche')}} class="p-0" >
                    <i class="fas fa-edit"></i>  Saisir une fiche
                  </a>
                </div>

                <div class="p-0 pb-2 col-12 liste  ">
                  <a href= {{path ('visiteur_frais')}}  class="p-0 " >
                    <i class="fas fa-file-alt"></i>  Suivre ses remboursements
                  </a>
                </div>

                <div class="p-0 pb-2 col-12 liste  ">
                  <a href= {{path ('vehicule')}}   class="p-0  " >
                    <i class="fas fa-car"></i>  Saisir un nouveau véhicule
                  </a>
                </div>
            </div>
          </div>
        <div class="col-10 p-0 margin-left">
        <div class="row">
        <div class="col">
          <nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
            <div class="container-fluid px-0">
              <a class="navbar-brand" href={{path ('accueil_visiteurs')}}>GSB | Galaxy Swiss Bourdin</a>
              <div class="navbar-brand" id="navbarNavDropdown">
                <ul class="navbar-nav">
                  <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/logout">Déconnexion <i class="fas fa-sign-in-alt"></i></a>   
                  </li>
                </ul>
              </div>
            </div>
          </nav> 
        </div>
      </div>
          <div class="container-fluid">
          <div class="row top">
            <div class="my-auto text_bloc_top col col-md-8 col-lg-9 p-0 bloc_top">
              <p class="m-0 ml-2 titre_bandeau">Saisir une fiche de frais</p>
            </div>
            <div class="my-auto text_bloc_top col col-md-4 col-lg-3 p-0 bloc_top">
              <p class="m-0">Période : {{date}}</p>
            </div>
          </div>
            <div class="row mx-auto w-75 p-0 mt-5">
                <div class="col">
                {% for vehicule in vehicule %}
                  {% if loop.last %}

                  {% if loop.index > 0 %}
                            
                {{ form_start(form) }}

                    <div class="card">
                        <div class="card-header bg-info title">
                            Frais au forfait
                        </div>
                        <div class="card-body">

                          <form action= {{ path('frais_forfait') }} method="POST">

                          <div class="row">
                            <div class="col m-1"> {{ form_row (form.repasMidi)}} </div>
                            <div class="col m-1"> {{ form_row (form.nuit)}}      </div>
                            <div class="col m-1"> {{ form_row (form.etape)}}     </div>
                            <div class="col m-1"> {{ form_row (form.km)}}        </div>
                            <div class="col m-1"> {{ form_row (form.libelle)}}   </div>
                            {% do form.montant.setRendered() %}
                            {% do form.proprietaire.setRendered() %}
                            {% do form.mois.setRendered() %}
                            {% do form.etat.setRendered() %}
                          </div>

                          <div class="row">
                            <div class="col-8 col-sm-5 col-md-4 col-lg-3 col-xl-2">
                              <button class="btn btn-outline-success mt-3" type="submit"> Ajouter </button>

                            </div>
                              <div class="col">
                            </div>
                          </div>
                    </div>
                </div>
                
            </div>
        </div>
        
        {{ form_end(form) }}    
{% endif %}
                {% endif %}
                {% else %}
                <div class="row justify-content-center">
                  <div class="card text-white bg-warning mb-3 col mx-3 px-0" style="max-width: 27rem;">
                      <div class="card-header" style="font-weight: bold; font-size: 20px;">Aucun véhicule associé à votre compte</div>
                        <div class="card-body">
                              <a href="/visiteurs/vehicule" style="text-decoration: none; color: white; font-size: 18px;">Saisir un nouveau véhicule</a>
                        </div>
                      </div>
                      </div>
              {% endfor %}     
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous">
  {% endblock %}
</html>