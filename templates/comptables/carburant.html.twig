{% extends 'base_comptable.html.twig' %}

 {% block body %}
        <div class="row mx-auto w-75 p-0 mt-5">
            <div class="col">
              {# {% for carburant in carburant %}      #}
                    {{ form_start(form) }}
                      <div class="card">
                        <div class="card-header bg-info title"> Ajouter un carburant </div>
                          <div class="card-body">
                            <form action= {{ path('frais_forfait') }} method="POST">
                              <div class="row">
                                <div class="col m-1"> {{ form_row (form.nom)}} </div>
                                <div class="col m-1"> {{ form_row (form.prix)}}      </div>
                              </div>
                              <div class="row">
                                <div class="col-8 col-sm-5 col-md-4 col-lg-3 col-xl-2">
                                  <button class="btn btn-outline-success mt-3" type="submit"> Ajouter </button>
                                </div>
                                <div class="col"></div>
                              </div>
                          </div> 
                        </div>
                      </div>
                    {{ form_end(form) }}
            </div>
          </div>
{% endblock %}