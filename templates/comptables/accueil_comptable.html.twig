<!DOCTYPE html>
<html>
  <head>
    <title>GSB | Galaxy Swiss Bourdin</title>
    <meta charset='utf-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    {% block stylesheets %} 
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" crossorigin="anonymous">

            <link href="{{asset ('styles/main.css') }}"     rel="stylesheet"/>
            <link href="{{asset ('styles/overflow.css') }}" rel="stylesheet"/>
            <link href="{{asset ('styles/all.css') }}"      rel="stylesheet"/> 
    {% endblock %}
  </head>

  {% block body %}
    <div class="container-fluid px-0">
      <div class="row">
        <div class="col">
          <nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
            <div class="container-fluid px-0">
              <a class="navbar-brand" href="#">GSB | Galaxy Swiss Bourdin</a>
              <div class="navbar-brand" id="navbarNavDropdown">
                <ul class="navbar-nav">
                  <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/logout">Déconnexion <i class="fas fa-sign-in-alt"></i></a>   
                  </li>
                </ul>
              </div>
            </div>
          </nav> 
        </div>
      </div>
      <div class="row">
        <div class="col-2 sidebar p-0">
              <header>
                <a href="#">
                  {% if app.user is null %}
                    <h3 class="my-3">Aucun utilisateur connecté</h3>
                  {% else %}
                    <h3 class="py-3">Bienvenue {{ app.user.prenom }}</h3>
                  {% endif %}<span class="id_user">
                </a>
              </header>
              <div class="ml-3 nav w-auto pl-3 mt-4">
                <div class="p-0 pb-2 col-12 liste">
                  <a href="/comptables/accueil_comptable" class="p-0" >
                    <i class="fas fa-home"></i>Accueil
                  </a>
                </div>

                <div class="p-0 pb-2 col-12 liste">
                  <a href="/comptables/fiche_comptable" class="p-0" >
                    <i class="fas fa-users"></i>Visiteurs
                  </a>
                </div>

                <div class="p-0 pb-2 col-12 liste">
                  <a href="/comptables/fiche_frais" class="p-0 " >
                    <i class="fas fa-file-alt"></i> Fiche de frais
                  </a>
                </div>
          </div>
        </div>

      <div class="col-10 p-0 margin-left">
          <div class="container-fluid">
            <div class="row page-header text-center pt-5 justify-content-center"> <h1> Bienvenue <span class="id_user">{% if app.user is null %}
                  {% else %}
                    {{ app.user.prenom }}
                  {% endif %}</span> sur votre espace comptable</h1> </div>        
                  <div class="row mt-5 justify-content-center">
                    <div class="card text-white bg-success mb-3 col px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais forfaitaires validés <i class="far fa-check-circle"></i></div>
                        <div class="card-body">
                          {% for valide in valide %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>

                    <div class="card text-white bg-warning mb-3 col mx-3 px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais forfaitaires en attente <i class="far fa-pause-circle"></i></div>
                        <div class="card-body">
                          {% for attente in attente %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>

                    <div class="card text-white bg-danger mb-3 col px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais forfaitaires rejetés <i class="fas fa-exclamation-triangle"></i></div>
                        <div class="card-body">
                          {% for rejete in rejete %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>
                    </div>
                    <div class="row mt-5 justify-content-center">
                    <div class="card text-white bg-success mb-3 col px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais hors forfait validés <i class="far fa-check-circle"></i></div>
                        <div class="card-body">
                          {% for valideBIS in valideBIS %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>

                    <div class="card text-white bg-warning mb-3 col mx-3 px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais hors forfait en attente <i class="far fa-pause-circle"></i></div>
                        <div class="card-body">
                          {% for attenteBIS in attenteBIS %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>

                    <div class="card text-white bg-danger mb-3 col px-0" style="max-width: 19rem;">
                      <div class="card-header" style="font-weight: bold; font-size: large">Frais hors forfait rejetés <i class="fas fa-exclamation-triangle"></i></div>
                        <div class="card-body">
                          {% for rejeteBIS in rejeteBIS %}
                            {% if loop.last %}
                              <p class="card-text" style="font-size: 60px;">{{loop.index}}</p>
                            {% endif %}
                            {% else %}
                              <p class="card-text" style="font-size: 60px;">0</p>
                          {% endfor %}
                        </div>
                      </div>
                    </div>
                  </div>
                  </div>
      </div>
    </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous">
  {% endblock %}
</html>